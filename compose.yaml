services:
  storybook:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "6006:6006"
    volumes:
      - .:/app
      - /app/node_modules
    command: ["npx", "storybook", "dev", "-p", "6006", "--host", "0.0.0.0", "--no-open"]
