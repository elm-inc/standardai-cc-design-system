services:
  storybook:
    image: node:24-slim
    working_dir: /app
    ports:
      - "6006:6006"
    volumes:
      - .:/app
    command: ["npx", "storybook", "dev", "-p", "6006", "--host", "0.0.0.0", "--no-open"]
